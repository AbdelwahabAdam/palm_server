apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: palm-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Uncomment for production with specific storage class
  # storageClassName: standard