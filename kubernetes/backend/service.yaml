apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: palm-app
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - port: 6543
      targetPort: 6543
  type: ClusterIP